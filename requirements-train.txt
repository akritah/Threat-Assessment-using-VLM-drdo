# Training and optional HuggingFace inference dependencies.
# Install in the project venv (recommended):
#   .\.venv\Scripts\python.exe -m pip install -r requirements-train.txt
#
# torch and torchvision must be installed as a matched pair to avoid resolver conflicts.

torch==2.12.0
torchvision==0.27.0
transformers>=4.49.0
peft>=0.14.0
trl>=0.15.0
bitsandbytes==0.48.0
accelerate>=1.2.0
datasets>=3.2.0
pyyaml>=6.0.0
pillow>=10.0.0
sentencepiece>=0.2.0
